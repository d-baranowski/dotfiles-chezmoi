# Plugins - managed by TPM and oh-my-zsh, not chezmoi
.tmux/plugins/
.oh-my-zsh/custom/plugins/

# Neovim lock file - managed by lazy.nvim, not chezmoi
.config/nvim/lazy-lock.json

# Claude Code settings — written at runtime by Claude Code (model selection, UI toggles,
# etc.). We do NOT own this file, but we do need to inject the `statusLine` key on apply,
# so it's handled via a modify_ script (dot_claude/modify_executable_settings.json) rather
# than being listed here. Other keys Claude writes are preserved by that script.

# Repo-meta docs — not dotfiles, should not be copied to $HOME
AGENTS.md
CLAUDE.md
README.md

# Machine-local Claude Code skills — not synced across machines.
# Add any skill here that should live only on this host.
.claude/skills/keybind

# Slackdump export accidentally committed 2026-05-19 (103 MB: sqlite + image
# uploads). Ignored so `chezmoi apply` doesn't splat it into $HOME. The sync
# LaunchAgent has been Disabled since 2026-04-19. Remove from the repo with
# `git rm -r --cached slackdump_20260519_140437` when convenient.
slackdump_20260519_140437/
